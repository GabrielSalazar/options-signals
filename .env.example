# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Telegram (opcional) — ATENÇÃO: há duas variáveis distintas no código para o
# token do bot (achado de uma análise arquitetural, não corrigido ainda):
#   - backend/core/settings.py (MotorSettings, usado por CONFIG) lê TELEGRAM_TOKEN
#   - backend/services/telegram_service.py lê TELEGRAM_BOT_TOKEN diretamente
# Configure AMBAS até essa inconsistência ser investigada/unificada.
TELEGRAM_TOKEN=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Dados de mercado
BRAPI_TOKEN=

# Redis (opcional — fallback em memória local se ausente)
REDIS_URL=

# CORS
ALLOWED_ORIGINS=

# Frontend (Next.js) — URL do backend FastAPI
API_URL=
NEXT_PUBLIC_API_URL=
